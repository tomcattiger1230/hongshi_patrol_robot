# 2026-09-23 实车与远程 GUI 开发总结

本次工作把此前分散的摄像头、雷达建图、升降平台和公网连接能力整合进 macOS
PySide6 GUI，并在 Robot320 NUC、云服务器和本地 Mac 三端完成部署及实链路验证。

## 已完成

### 综合 GUI

- 新增两路海康监控画面，统一卡片尺寸、状态栏和局域网/云端/关闭链路选择。
- 云端画面转码为 H.264、960×540、12 FPS、每路约 600 kbit/s；局域网保留
  2560×1440 原始画面。
- 新增两台海康 USB 工业相机按需静态抓图。未点击时不采集、不上传；两台均实测返回
  4096×2460 JPEG。界面提供自动、局域网和公网链路；自动模式局域网失败后回退公网。
  冷启动抓图改为至少 5 秒自动曝光预热，修复首次图片明显偏暗的问题。
- 新增雷达、轨迹、栅格地图显示、建图启停及地图保存到 GUI 本机的界面。
- 新增升降平台“上升/下降/停止”实际控制页。每次点击发送一次 RS-485 指令，允许重复
  点击；界面确认只表示串口写入成功。

### 车端服务

- 部署 MID-360 只读采集、空间数据桥接和独立 Cartographer 建图服务。
- 部署两台工业相机 MVS SDK 单帧抓图脚本。
- 部署升降平台常驻串口桥接及受限控制入口。
- 部署两路监控视频推流和本地 RTSP 代理，网络切换后自动重建推流连接。

### Wi-Fi 优先、SIM 自动备用

- NUC Wi-Fi 健康时使用 route metric 100；有人 G809 有线/SIM 保持 metric 600。
- 每 5 秒检查一次 Wi-Fi 到云服务器的连通性。连续 3 次失败后把 Wi-Fi 改为 metric 900，
  由 SIM 接管；连续 3 次成功后自动恢复 Wi-Fi 优先。
- 为 `192.168.1.50` 增加独立源策略路由，使 SIM 备用隧道在 Wi-Fi 为默认出口时仍可
  同时在线。
- NUC 同时维持 Wi-Fi 与 SIM 两条反向 SSH 隧道。云端 loopback 选择器验证真实 SSH
  banner，优先 Wi-Fi，失败时回退到 SIM；机器人 SSH 端口不直接暴露到公网。

## 实车验证结果

| 功能 | Wi-Fi | 有线/SIM |
|---|---:|---:|
| 云端反向 SSH | 通过 | 通过 |
| 两路监控视频推送与 Mac JPEG | 通过 | 通过 |
| 两台工业相机静态抓图 | 通过 | 通过 |
| 升降平台“停止”安全指令 | 通过 | 通过 |
| MID-360 与车上 Cartographer | 通过 | 切换期间持续运行 |

完整断网测试中，NUC 断开 Wi-Fi 后自动改用 `enp85s0`，两路 FFmpeg 连接从 SIM 地址
重新发布到云端，控制和静态抓图继续可用。Wi-Fi 恢复并连续通过健康检查后，默认路由、
两路视频和首选反向隧道自动切回 Wi-Fi；SIM 隧道保持在线待命。测试过程没有发送底盘
行驶或 PTZ 移动指令，升降平台只发送“停止”。

## 当前边界与后续工作

- 监控 PTZ 的 HTTP Agent 仍主要面向局域网，尚未迁入双反向 SSH/WAN 通道。
- 雷达、地图、轨迹和建图管理已经增加受限 SSH/WAN 数据流；macOS/Windows 不再依赖
  跨公网 DDS discovery。点云已分别通过 Wi-Fi 和 SIM 实测，SIM 上完成建图开始、地图
  回传和停止闭环。
- 长时间 Cartographer 会话超过 1,000,000 栅格时，车端现在会自动提高预览分辨率并按块
  保守降采样，避免直接丢弃地图或占满公网带宽。
- GUI 底盘/导航仍为离线 demo，实车安全门控和跨公网控制尚未授权上线。
- 两台 USB 工业相机当前完成静态抓图，双路录像任务、磁盘保护和远程录像状态仍待开发。

## 验证

- Python 测试：95 项通过。
- 本次相关 Python 文件 Ruff 检查通过。
- Shell 脚本语法检查和 `git diff --check` 通过。
- 全仓库 Ruff 仍有 Livox 上游 launch 文件及旧 CAN 脚本的既有告警，本次未改动这些文件。

详细运行和部署说明见：

- `remote_control/README.md`
- `video_relay/README.md`
- `scripts/robot320_onboard/README.md`
